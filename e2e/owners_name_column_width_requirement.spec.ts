// Generated by Skyramp v1.3.40 on 2026-09-02 08:02:59.084279619 +0000 UTC m=+1082.954238927
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owners_name_column_width_requirement.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owners_name_column_width_requirement_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';
import { collectWidthUtilityRuleSelectors } from './skyrampUtils';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(60000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    const pageErrors: string[] = [];
    page.on('pageerror', (error: Error) => pageErrors.push(error.message));

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();
    await page.waitForLoadState('networkidle');
    await expect(page).toHaveURL(`${baseUrl}/petclinic/owners`);
    await page.locator("#ownersTable").waitFor({ state: 'visible', timeout: 10000 });
    await expect(page.locator("#ownersTable table thead th")).toHaveCount(5);
    await expect(page.locator("#ownersTable table thead th").nth(0)).toHaveText("Name");
    await expect(page.locator("#ownersTable table thead th").nth(0)).toHaveClass(/w-20/);
    await expect(page.locator("#ownersTable table thead th").nth(1)).toHaveClass(/w-25/);

    const widthRuleSelectors = await collectWidthUtilityRuleSelectors(page);
    expect(widthRuleSelectors, "the Address header's w-25 class must resolve to a real CSS width rule").toContain(".w-25");
    expect(widthRuleSelectors, "the PR description says the name column is widened, so its w-20 class must resolve to a real CSS width rule").toContain(".w-20");

    expect(pageErrors, 'no uncaught page errors').toEqual([]);
});
