// Generated by Skyramp v1.3.40 on 2026-08-30 08:51:22.484740562 +0000 UTC m=+1138.678798266
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owner-edit-prepopulate-and-save.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owner_edit_prepopulate_and_save_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';
import { openEditOwnerForm, submitOwnerForm } from './skyrampUtils';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(141000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();
    await page.locator("a[href=\"/petclinic/owners/1\"]").click();

    await openEditOwnerForm(page);

    await expect(page.locator("#firstName")).toHaveValue("George");
    await expect(page.locator("#lastName")).toHaveValue("Franklin");
    await expect(page.locator("#address")).toHaveValue("110 W. Liberty St.");
    await expect(page.locator("#city")).toHaveValue("Madison");
    await expect(page.locator("#telephone")).toHaveValue("6085551023");

    await submitOwnerForm(page, "George", "Franklin", "110 W. Liberty St.", "Madison", "6085551023");

    await expect(page.getByRole("heading", { name: "Owner Information" })).toBeVisible();
    await expect(page).toHaveURL(`${baseUrl}/petclinic/owners/1`);
});
