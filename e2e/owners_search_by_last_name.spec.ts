// Generated by Skyramp v1.3.40 on 2026-08-30 08:45:38.006044584 +0000 UTC m=+847.382864498
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owners_search_by_last_name.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owners_search_by_last_name_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';
import { fillOwnerLastNameFilter, submitOwnerSearch } from './skyrampUtils';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(81000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();

    await fillOwnerLastNameFilter(page, "Davis");
    await submitOwnerSearch(page);
    await expect(page.getByRole("textbox", { name: "Last name" })).toHaveValue("Davis");

    // Both seeded owners whose last name starts with "Davis" are listed.
    await expect(page.getByRole("link", { name: "Betty Davis" })).toBeVisible();
    await expect(page.getByRole("link", { name: "Harold Davis" })).toBeVisible();
    // Non-matching owners are filtered out -- catches a search that silently
    // returns the unfiltered list.
    await expect(page.getByRole("link", { name: "George Franklin" })).toHaveCount(0);
    await expect(page.getByRole("link", { name: "Peter McTavish" })).toHaveCount(0);
});
