// Generated by Skyramp v1.3.40 on 2026-08-30 08:46:30.402810025 +0000 UTC m=+872.901240912
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output visit-add-description-over-limit-rejected.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/visit_add_description_over_limit_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';
import { fillVisitForm } from './skyrampUtils';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(60000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.goto(`${baseUrl}/pets/1/visits/add`);
    await fillVisitForm(page, "2026-08-30", "yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy");
    await expect(page.getByRole("textbox", { name: "Description" })).toHaveValue("yyyyyyyyyyyyyyyyyyyyyyyyyyyyyy");
    await expect(page.locator("#description")).toHaveAttribute("maxlength", "30");
    await expect(page.getByRole("button", { name: "Add Visit" })).toBeEnabled();
    await expect(page.locator("#description")).not.toHaveClass(/is-invalid/);
    await expect(page).toHaveURL(/\/petclinic\/pets\/1\/visits\/add$/);
});
