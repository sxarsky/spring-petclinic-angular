// Generated by Skyramp on 2026-09-08 19:38:36.484 Coordinated Universal Time


import type { Page } from '@playwright/test';

export async function openAddPetForm(page: Page) {
    await page.getByRole("button", { name: "Add New Pet" }).click();
}

export async function fillPetForm(page: Page, name: string, birthDate: string, typeName: string) {
    await page.locator("#name").fill(name);
    await page.locator("#birthDate").fill(birthDate);
    await page.getByLabel("Type").selectOption(typeName);
}

export async function submitPetForm(page: Page) {
    await page.getByRole("button", { name: "Save Pet" }).click();
}
