// Generated by Skyramp on 2026-09-08 19:18:33.361 Coordinated Universal Time


import type { Page, Response } from '@playwright/test';
import { expect } from '@skyramp/skyramp';

export async function openAddOwnerForm(page: Page) {
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Add New" }).click();
}

export async function fillOwnerForm(
    page: Page,
    firstName: string,
    lastName: string,
    address: string,
    city: string,
    telephone: string,
    email: string
) {
    await page.locator("#firstName").fill(firstName);
    await page.locator("#lastName").fill(lastName);
    await page.locator("#address").fill(address);
    await page.locator("#city").fill(city);
    await page.locator("#telephone").fill(telephone);
    await page.locator("#email").fill(email);
    await expect(page.getByRole("button", { name: "Add Owner" })).toBeEnabled();
}

export async function submitAddOwnerForm(page: Page): Promise<Response> {
    const responsePromise0 = page.waitForResponse("**/petclinic/api/owners**");
    await page.getByRole("button", { name: "Add Owner" }).click();
    const response0 = await responsePromise0;
    return response0;
}
