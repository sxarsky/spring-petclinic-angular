// Generated by Skyramp on 2026-09-02 08:10:19.429 Coordinated Universal Time

import type { Page } from '@playwright/test';

export async function fillOwnerForm(page: Page, firstName: string, lastName: string, address: string, city: string, phoneNumber: string) {
    await page.locator("#firstName").fill(firstName);
    await page.locator("#lastName").fill(lastName);
    await page.locator("#address").fill(address);
    await page.locator("#city").fill(city);
    await page.locator("#phoneNumber").fill(phoneNumber);
}

export async function submitOwnerForm(page: Page) {
    const responsePromise = page.waitForResponse("**/petclinic/api/owners**");
    await page.getByRole("button", { name: "Add Owner" }).click();
    return await responsePromise;
}
