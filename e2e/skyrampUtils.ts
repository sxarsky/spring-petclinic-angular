// Generated by Skyramp on 2026-09-08 19:14:46.339 Coordinated Universal Time


import type { Page } from '@playwright/test';
import { getBaseUrl } from '@skyramp/skyramp';

// URL for the Angular app, which proxies /petclinic/** to the REST service.
export const URL_app = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

export async function fillVisitForm(page: Page, date: string, description: string) {
    await page.locator("#date").fill(date);
    await page.locator("#description").fill(description);
}

export async function submitVisitForm(page: Page) {
    const responsePromise0 = page.waitForResponse("**/petclinic/api/owners/**/pets/**/visits**");
    await page.getByRole("button", { name: "Add Visit" }).click();
    return await responsePromise0;
}

export async function deleteVisit(page: Page, visitId: number) {
    return await page.request.delete(`${URL_app}/petclinic/api/visits/${visitId}`);
}
