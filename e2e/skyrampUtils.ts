// Generated by Skyramp on 2026-08-29 09:22:47.483 Coordinated Universal Time


export async function openAddOwnerForm(page) {
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Add New" }).click();
}

export async function submitOwnerForm(page, firstName, lastName, address, city, telephone) {
    await page.locator("#firstName").fill(firstName);
    await page.locator("#lastName").fill(lastName);
    await page.locator("#address").fill(address);
    await page.locator("#city").fill(city);
    await page.locator("#telephone").fill(telephone);
    // Wait to finish any potential hydration
    await page.waitForTimeout(1500);
    const responsePromise = page.waitForResponse("**/petclinic/api/owners**");
    await page.getByRole("button", { name: "Add Owner" }).click();
    const response = await responsePromise;

    // Wait for network activities to finish
    await page.waitForTimeout(3000);

    return response;
}

export async function fillOwnerTelephone(page, telephone) {
    await page.locator("#telephone").fill(telephone);
}

export async function openOwnersList(page) {
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();
}

export async function openEditOwnerForm(page) {
    await page.locator("a[href=\"/petclinic/owners/1\"]").click();
    await page.getByRole("button", { name: "Edit Owner" }).click();
    await page.waitForTimeout(3000);
}
