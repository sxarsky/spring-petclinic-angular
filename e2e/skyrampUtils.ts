// Generated by Skyramp on 2026-09-09 05:53:22.651 Coordinated Universal Time


import { expect } from '@skyramp/skyramp';
import type { Page } from '@playwright/test';

export async function openAddVisitForm(page: Page) {
    await page.getByRole("button", { name: "Add Visit" }).click();
}

export async function submitVisitForm(page: Page, date: string, description: string) {
    await page.locator("#date").fill(date);
    await page.locator("#description").fill(description);
    // The submit button stays disabled until the form is valid, so wait on the control
    // the next line clicks rather than on a fixed sleep.
    await expect(page.getByRole("button", { name: "Add Visit" })).toBeEnabled();
    const responsePromise0 = page.waitForResponse("**/petclinic/api/owners/**/pets/**/visits**");
    await page.getByRole("button", { name: "Add Visit" }).click();
    const response0 = await responsePromise0;
    return response0;
}

export async function deleteVisit(page: Page) {
    await page.getByRole("button", { name: "Delete Visit" }).click();
}
