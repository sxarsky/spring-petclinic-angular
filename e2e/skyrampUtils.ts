// Generated by Skyramp on 2026-09-09 05:04:54.668 Coordinated Universal Time


import { expect } from '@playwright/test';
import type { Page } from '@playwright/test';

export async function openAddVisitForm(page: Page, petIndex: number) {
    await page.getByRole("button", { name: "Add Visit" }).nth(petIndex).click();
}

export async function fillVisitForm(page: Page, date: string, description: string, durationMinutes: number) {
    await page.locator("#date").fill(date);
    await page.locator("#description").fill(description);
    await page.locator("#durationMinutes").fill(durationMinutes.toString());
}

export async function submitAddVisitForm(page: Page) {
    await expect(page.getByRole("button", { name: "Add Visit" })).toBeEnabled();
    const responsePromise = page.waitForResponse("**/petclinic/api/owners/**/pets/**/visits**");
    await page.getByRole("button", { name: "Add Visit" }).click();
    return await responsePromise;
}

export async function openEditVisitForm(page: Page, rowIndex: number) {
    await page.locator("tr").nth(rowIndex).getByRole("button", { name: "Edit Visit" }).click();
}

export async function setVisitDuration(page: Page, durationMinutes: number) {
    await page.locator("#durationMinutes").fill(durationMinutes.toString());
}

export async function submitEditVisitForm(page: Page) {
    await page.getByRole("button", { name: "Update Visit" }).click();
}

export async function deleteVisitRow(page: Page, rowIndex: number) {
    await page.locator("tr").nth(rowIndex).getByRole("button", { name: "Delete Visit" }).click();
}
