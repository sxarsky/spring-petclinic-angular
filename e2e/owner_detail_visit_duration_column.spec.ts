// Generated by Skyramp v1.3.40 on 2026-08-29 09:22:13.694605378 +0000 UTC m=+853.824389111
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owner_detail_visit_duration_column.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owner_detail_visit_duration_column_trace.zip

// Import of required libraries
import { test, getBaseUrl, newSkyrampPlaywrightPage, expect } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(1313000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.waitForLoadState('networkidle');

    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();
    await page.waitForLoadState('networkidle');

    // The owners list is the one screen that renders fetched data today.
    await expect(page.getByRole("link", { name: "Jean Coleman" })).toHaveText("Jean Coleman");

    await page.locator("a[href=\"/petclinic/owners/6\"]").click();
    await page.waitForLoadState('networkidle');

    // The owner detail must render the owner it fetched.
    await expect(page.locator(".ownerFullName")).toHaveText("Jean Coleman");

    // This PR adds a Duration column to the visits table rendered inside
    // the owner's "Pets and Visits" section. It must be present.
    await expect(page.locator("app-owner-detail")).toContainText("Duration");
});
