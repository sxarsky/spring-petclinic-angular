// Generated by Skyramp v1.3.40 on 2026-08-29 06:01:38.470255659 +0000 UTC m=+759.962543634
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output vets-table-header-cell-count-parity.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/vets_table_header_cell_count_parity_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(63000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(`${baseUrl}/`);
    await page.getByRole("button", { name: "Veterinarians" }).click();
    await page.getByRole("link", { name: "All" }).click();
    await expect(page.locator("#vets tbody > tr").first().locator("td")).toHaveCount(3);
    await expect(page.locator("#vets thead > tr > th")).toHaveCount(3);
    const bodyCellsPerRow = await page.locator("#vets tbody > tr").first().locator("td").count();
    await expect(page.locator("#vets thead > tr > th")).toHaveCount(bodyCellsPerRow);
});
