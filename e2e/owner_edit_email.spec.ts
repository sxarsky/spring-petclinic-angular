// Generated by Skyramp v1.3.40 on 2026-08-29 04:20:02.937790269 +0000 UTC m=+1107.548298446
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owner_edit_email.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owner_edit_email_ui_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(1604000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.goto(`${baseUrl}/petclinic/owners`);
    // Wait for network activities to finish
    await page.waitForTimeout(5000);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();
    await page.locator("a[href=\"/petclinic/owners/1\"]").click();
    await page.getByRole("button", { name: "Edit Owner" }).click();
    await page.locator("#firstName").fill("George");
    await page.locator("#lastName").fill("Franklin");
    await page.locator("#address").fill("110 W. Liberty St.");
    await page.locator("#city").fill("Madison");
    await page.locator("#telephone").fill("6085551023");
    await page.locator("#email").fill("george.updated@example.com");
    await expect(page.getByRole("textbox", { name: "Email" })).toHaveValue("george.updated@example.com");
    await page.getByRole("button", { name: "Update Owner" }).click();
    await page.getByRole("button", { name: "Back" }).click();

    await expect(page).toHaveURL(`${baseUrl}/petclinic/owners`);

    // The owners table must reflect the email that was just saved on the edit form.
    // This currently FAILS: OwnerRestControllerV1.updateOwner copies address, city,
    // firstName, lastName and telephone onto the entity but never calls setEmail,
    // so PUT /api/owners/{ownerId} returns 204 while silently discarding the new email.
    const georgeRow = page
        .locator("#ownersTable table > tbody > tr")
        .filter({ has: page.getByRole("link", { name: "George Franklin" }) });
    await expect(georgeRow).toContainText("george.updated@example.com");
});
