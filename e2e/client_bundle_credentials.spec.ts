// Generated by Skyramp v1.3.40 on 2026-08-29 06:03:49.442470012 +0000 UTC m=+750.454294087
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output client_bundle_credentials.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/client_bundle_credentials_trace_1.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(60000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    const pageErrors: string[] = [];
    page.on('pageerror', (err) => pageErrors.push(err.message));
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(`${baseUrl}/`);
    await expect(page.locator('app-welcome h1')).toHaveText('Welcome to Petclinic');

    const bundle = await page.request.get(`${baseUrl}/main.js`);
    expect(bundle.status()).toBe(200);
    const bundleSource = await bundle.text();
    expect(bundleSource.includes('API_PASSWORD')).toBe(false);
    expect(bundleSource.includes('API_USERNAME')).toBe(false);
    expect(bundleSource.includes('btoa(`${environment.API_USERNAME}')).toBe(false);

    expect(pageErrors).toHaveLength(0);
});
