// Generated by Skyramp v1.3.40 on 2026-08-29 05:48:51.806106624 +0000 UTC m=+1278.221401072
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output visit_add_description_required_minlength_guard.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/visit_add_description_required_guard_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(62000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    // assertions reviewed: covers the required/minlength="1" guard on the Description input
    // changed by this PR. Asserts Add Visit is disabled on load and still disabled with only a
    // date, then enabled once a single character is entered, plus the field value itself.
    // No API response is captured in this flow, so the response-body rules do not apply.
    await page.goto(`${baseUrl}/pets/1/visits/add`);
    await expect(page.getByRole("button", { name: "Add Visit" })).toBeDisabled();
    await page.locator("#date").fill("2026-08-29");
    await expect(page.getByRole("button", { name: "Add Visit" })).toBeDisabled();
    await page.locator("#description").fill("X");
    await expect(page.getByRole("textbox", { name: "Description" })).toHaveValue("X");
    await expect(page.getByRole("button", { name: "Add Visit" })).toBeEnabled();
});
