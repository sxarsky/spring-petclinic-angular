// Generated by Skyramp v1.3.40 on 2026-08-29 03:34:33.330465413 +0000 UTC m=+991.595292982
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owner_add_email_appears_in_owners_table.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owner_add_email_appears_in_owners_table_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, getValue, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(1263000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.goto(`${baseUrl}/petclinic/owners`);
    // Wait for network activities to finish
    await page.waitForTimeout(5000);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Add New" }).click();

    const playwrightRequest0: Record<string, any> = {
        "address": "12 Analytical Way",
        "city": "Madison",
        "email": "ada.lovelace@example.com",
        "firstName": "Ada",
        "lastName": "Lovelace",
        "telephone": "6085551815"
    };

    await page.locator("#firstName").fill(getValue(playwrightRequest0, "firstName"));
    await page.locator("#lastName").fill(getValue(playwrightRequest0, "lastName"));
    await page.locator("#address").fill(getValue(playwrightRequest0, "address"));
    await page.locator("#city").fill(getValue(playwrightRequest0, "city"));
    await page.locator("#telephone").fill(getValue(playwrightRequest0, "telephone"));
    await page.locator("#email").fill(getValue(playwrightRequest0, "email"));
    await expect(page.getByRole("textbox", { name: "Email" })).toHaveValue("ada.lovelace@example.com");
    // Wait to finish any potential hydration
    await page.waitForTimeout(1500);
    const responsePromise0 = page.waitForResponse("**/petclinic/api/owners**");
    await page.getByRole("button", { name: "Add Owner" }).click();
    const response0 = await responsePromise0;

    // Wait for network activities to finish
    await page.waitForTimeout(3000);

    const responseBody0 = await response0.json();
    expect(response0.status()).toBe(201);
    expect(responseBody0.firstName).toBe("Ada");
    expect(responseBody0.lastName).toBe("Lovelace");
    expect(responseBody0.address).toBe("12 Analytical Way");
    expect(responseBody0.city).toBe("Madison");
    expect(responseBody0.telephone).toBe("6085551815");
    expect(responseBody0.email).toBe("ada.lovelace@example.com");
    expect(Number(responseBody0.id)).toBeGreaterThan(0);

    await expect(page.locator("#ownersTable thead tr").first()).toContainText("Email");
    await expect(page.getByRole("link", { name: "Ada Lovelace" }).first()).toBeVisible();
    await expect(page.locator("#ownersTable tbody tr").filter({ hasText: "Ada Lovelace" }).first()).toContainText("ada.lovelace@example.com");
});
