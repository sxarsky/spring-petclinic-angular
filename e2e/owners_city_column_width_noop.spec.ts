// Generated by Skyramp v1.3.40 on 2026-09-02 08:01:03.050845146 +0000 UTC m=+966.920804456
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owners_city_column_width_noop.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owners_city_column_width_noop_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';
import { collectWidthUtilityRuleSelectors } from './skyrampUtils';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(60000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    const pageErrors: string[] = [];
    page.on('pageerror', (error: Error) => pageErrors.push(error.message));

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();
    await page.waitForLoadState('networkidle');
    await page.locator("#ownersTable").waitFor({ state: 'visible', timeout: 10000 });
    await expect(page.locator("#ownersTable table thead th")).toHaveCount(5);
    await expect(page).toHaveURL(`${baseUrl}/petclinic/owners`);
    await expect(page.locator("#ownersTable table thead th").nth(2)).toHaveText("City");
    await expect(page.locator("#ownersTable table thead th").nth(2)).toHaveClass(/w-15/);

    const widthRuleSelectors = await collectWidthUtilityRuleSelectors(page);
    expect(widthRuleSelectors, "the City header's w-15 class must resolve to a real CSS width rule").toContain(".w-15");

    expect(pageErrors, 'no uncaught page errors').toEqual([]);
});
