// Generated by Skyramp v1.3.40 on 2026-08-29 09:21:59.291229997 +0000 UTC m=+851.378146868
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owners_list_renders_every_owner.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owners_list_renders_every_owner_trace.zip

// Import of required libraries
import { test, getBaseUrl, newSkyrampPlaywrightPage, expect } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(1181000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();

    await expect(page).toHaveURL(/\/petclinic\/owners$/);
    await expect(page.getByRole('heading', { name: 'Owners' })).toBeVisible();

    // The list rendered real owner data: first and last rows of the first page.
    await expect(page.getByRole('link', { name: 'George Franklin' })).toBeVisible();
    await expect(page.getByRole('link', { name: 'Peter McTavish' })).toBeVisible();

    // The owners list must show every owner in the clinic. The backend reports
    // totalElements=10 for GET /petclinic/api/v2/owners, but the page requests the
    // endpoint with no size parameter, so it renders only the default page of 5 and
    // provides no pagination control. These assertions FAIL on the current code and
    // document that owners past the first page are unreachable from this screen.
    await expect(page.locator('#ownersTable td.ownerFullName')).toHaveCount(10);
    await expect(page.getByRole('link', { name: 'Jean Coleman' })).toBeVisible();
    await expect(page.getByRole('link', { name: 'Carlos Estaban' })).toBeVisible();
});
