// Generated by Skyramp v1.3.40 on 2026-08-29 06:04:06.241766051 +0000 UTC m=+949.733134014
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output pet_add_nickname_submit.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/pet_add_nickname_submit_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, getValue, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(200000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();
    await page.locator("a[href=\"/petclinic/owners/1\"]").click();
    await page.getByRole("button", { name: "Add New Pet" }).click();

    const playwrightRequest0: Record<string, any> = {
        "birthDate": "2020-01-15",
        "name": "Rex",
        "nickname": "Buddy",
        "type": {"name": "cat"}
    };

    await page.locator("#name").fill(getValue(playwrightRequest0, "name"));
    await page.locator("#nickname").fill(getValue(playwrightRequest0, "nickname"));
    await expect(page.getByRole("textbox", { name: "Nickname" })).toHaveValue("Buddy");
    await page.locator("#birthDate").fill(getValue(playwrightRequest0, "birthDate"));
    await page.getByLabel("Type").selectOption(getValue(playwrightRequest0, "type.name"));
    // Wait to finish any potential hydration
    await page.waitForTimeout(1500);
    const responsePromise0 = page.waitForResponse("**/petclinic/api/owners/**/pets**");
    await page.getByRole("button", { name: "Save Pet" }).click();
    const response0 = await responsePromise0;

    expect(response0.status()).toBe(201);
    const createdPet = await response0.json();
    expect(createdPet.name).toBe(getValue(playwrightRequest0, "name"));
    expect(createdPet.nickname).toBe(getValue(playwrightRequest0, "nickname"));
    expect(createdPet.birthDate).toBe(getValue(playwrightRequest0, "birthDate"));
    expect(createdPet.type.name).toBe(getValue(playwrightRequest0, "type.name"));
    expect(createdPet.ownerId).toBe(1);
    expect(createdPet.id).toBeGreaterThan(0);

    // Wait for network activities to finish
    await page.waitForTimeout(3000);

    await expect(page.getByRole("heading", { name: "Pets and Visits" })).toBeVisible();
});
