// Generated by Skyramp v1.3.40 on 2026-08-29 09:22:13.791877663 +0000 UTC m=+878.140179167
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owner_list_empty_search_alert.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owner_list_empty_search_alert_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';
import { searchOwnersByLastName } from './skyrampUtils';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(1275000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.goto(`${baseUrl}/petclinic/owners`);
    // Wait for network activities to finish
    await page.waitForTimeout(5000);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();

    await searchOwnersByLastName(page, "Zzzznotanowner");

    await expect(page.getByText("No owners found with a last")).toContainText("No owners found with a last name starting with \"Zzzznotanowner\". Try a shorter search, or clear the box to list everyone.");
    await expect(page.getByRole("textbox", { name: "Last name" })).toHaveValue("Zzzznotanowner");
    await expect(page.locator("#ownersTable")).toHaveCount(0);
    await expect(page.getByRole("button", { name: "Add Owner" })).not.toBeVisible();
});
