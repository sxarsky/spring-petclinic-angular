// Generated by Skyramp v1.3.40 on 2026-08-29 09:25:50.412166468 +0000 UTC m=+1054.717331305
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owner_search_clear_recovers_from_no_results.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owner_search_clear_recovers_from_no_results_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';
import { searchOwnersByLastName, clearOwnerSearch } from './skyrampUtils';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(72000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();

    await searchOwnersByLastName(page, "Nonexistent");
    await expect(page.getByText("No owners with LastName")).toContainText("No owners with LastName starting with \"Nonexistent\"");
    await expect(page.locator("#ownersTable")).toHaveCount(0);

    await clearOwnerSearch(page);
    await expect(page.getByRole("row", { name: "George Franklin 110 W." })).toContainText("George Franklin");
    await expect(page.locator("#lastName")).toHaveValue("");
    await expect(page.locator("#ownersTable tbody > tr")).toHaveCount(10);
});
