// Generated by Skyramp v1.3.40 on 2026-08-29 03:34:41.082392278 +0000 UTC m=+999.347219847
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owner_edit_email_round_trip_ui.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owner_edit_email_round_trip_ui_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(193000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();
    await page.locator("a[href=\"/petclinic/owners/2\"]").click();
    await page.getByRole("button", { name: "Edit Owner" }).click();
    await page.locator("#firstName").fill("Betty");
    await page.locator("#lastName").fill("Davis");
    await page.locator("#address").fill("638 Cardinal Ave.");
    await page.locator("#city").fill("Sun Prairie");
    await page.locator("#telephone").fill("6085551749");
    await page.locator("#email").fill("betty.updated@example.com");
    await expect(page.getByRole("textbox", { name: "Email" })).toHaveValue("betty.updated@example.com");
    await page.getByRole("button", { name: "Update Owner" }).click();
    await page.getByRole("button", { name: "Back" }).click();

    await expect(page).toHaveURL(/\/petclinic\/owners$/);
    await expect(page.locator("#ownersTable tbody tr").filter({ hasText: "Betty Davis" }).first()).toContainText("6085551749");
    await expect(page.locator("#ownersTable tbody tr").filter({ hasText: "Betty Davis" }).first()).toContainText("betty.updated@example.com");
});
