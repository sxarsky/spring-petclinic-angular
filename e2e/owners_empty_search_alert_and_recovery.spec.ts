// Generated by Skyramp v1.3.40 on 2026-08-30 08:42:17.64552056 +0000 UTC m=+636.017629282
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owners_empty_search_alert_and_recovery.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owners_empty_search_alert_and_recovery_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';
import { searchOwnersByLastName } from './skyrampUtils';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(1075000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();

    await searchOwnersByLastName(page, "Zzz");
    await expect(page.getByText("No owners found with a last")).toContainText("No owners found with a last name starting with \"Zzz\". Try a shorter search, or clear the box to list everyone.");
    await expect(page.locator("#ownersTable")).toHaveCount(0);
    await expect(page.getByRole("button", { name: "Add Owner" })).not.toBeVisible();

    await searchOwnersByLastName(page, "");
    await expect(page.getByRole("link", { name: "George Franklin" })).toBeVisible();
    await expect(page.getByRole("row", { name: "George Franklin 110 W." })).toContainText("110 W. Liberty St.");
});
