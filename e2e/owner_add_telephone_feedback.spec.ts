// Generated by Skyramp v1.3.44 on 2026-09-08 19:11:11.26657963 +0000 UTC m=+1102.766970001
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owner_add_telephone_feedback.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owner_add_telephone_rejection_trace.zip

// Import of required libraries
import { openAddOwnerForm, fillOwnerForm, submitAddOwnerForm } from './skyrampUtils';
import { test, expect, getBaseUrl, getValue, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(269000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(`${baseUrl}/petclinic/owners/add`);
    // Wait for network activities to finish
    await page.waitForTimeout(5000);

    await page.goto(`${baseUrl}/`);
    await openAddOwnerForm(page);

    const playwrightRequest0: Record<string, any> = {
        "address": "1 Helper Text Way",
        "city": "Madison",
        "firstName": "Testbot",
        "lastName": "Ninedigit",
        "telephone": "608555102"
    };

    await fillOwnerForm(
        page,
        getValue(playwrightRequest0, "firstName"),
        getValue(playwrightRequest0, "lastName"),
        getValue(playwrightRequest0, "address"),
        getValue(playwrightRequest0, "city"),
        getValue(playwrightRequest0, "telephone"),
    );
    await expect(page.getByText("Digits only, no spaces or")).toContainText("Digits only, no spaces or dashes.");
    await expect(page.getByRole("button", { name: "Add Owner" })).toBeEnabled();

    const response0 = await submitAddOwnerForm(page, "**/petclinic/api/owners**");

    expect(response0.status()).toBe(400);
    const response0Body = await response0.json();
    expect(response0Body.status).toBe(400);
    expect(response0Body.title).toBe("MethodArgumentNotValidException");
    expect(response0Body.detail).toBe("The request contains invalid or missing parameters");
    expect(response0Body.instance).toBe("/petclinic/api/owners");
    expect(response0Body.schemaValidationErrors.length).toBe(2);
    expect(response0Body.schemaValidationErrors.every((e: any) => e.field === "telephone")).toBe(true);
    expect(response0Body.schemaValidationErrors.every((e: any) => e.rejectedValue === getValue(playwrightRequest0, "telephone"))).toBe(true);
    expect(response0Body.schemaValidationErrors[2]).toBeUndefined();

    await expect(page.locator(".alert-danger")).toBeVisible();
});
