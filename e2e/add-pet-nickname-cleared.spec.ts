// Generated by Skyramp v1.3.44 on 2026-09-08 19:31:44.747821191 +0000 UTC m=+2350.235824090
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output add-pet-nickname-cleared.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/add_pet_nickname_cleared_trace_2.zip

// Import of required libraries
import { test, expect, getBaseUrl, getValue, newSkyrampPlaywrightPage } from '@skyramp/skyramp';
import { openAddPetForm, fillPetForm, submitPetForm } from './skyrampUtils';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(1076000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(`${baseUrl}/`);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();
    await page.locator("a[href=\"/petclinic/owners/1\"]").click();
    await openAddPetForm(page);

    const playwrightRequest0: Record<string, any> = {
        "birthDate": "2020-01-01",
        "name": "NickCleared",
        "type": {"name": "cat"}
    };

    await fillPetForm(
        page,
        getValue(playwrightRequest0, "name"),
        getValue(playwrightRequest0, "birthDate"),
        getValue(playwrightRequest0, "type.name")
    );
    await page.locator("#nickname").fill("Temporary");
    await page.keyboard.press("Control+a");
    await page.keyboard.press("Delete");
    // Wait to finish any potential hydration
    await page.waitForTimeout(1500);
    const responsePromise0 = page.waitForResponse("**/petclinic/api/owners/**/pets**");
    await submitPetForm(page);
    const response0 = await responsePromise0;
    expect(response0.status(), 'an emptied optional Nickname must not make the save fail').toBe(201);
    expect(await response0.json(), 'the saved pet').toMatchObject({ name: "NickCleared", birthDate: "2020-01-01" });
    await expect(page.getByRole("heading", { name: "Owner Information" })).toBeVisible();
});
