// Generated by Skyramp v1.3.40 on 2026-08-29 05:50:47.370496806 +0000 UTC m=+1393.785791254
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output visit_edit_description_over_limit_save_error.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/visit_edit_description_over_limit_trace_v2.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(60000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    // assertions reviewed: this test documents a defect and is EXPECTED TO FAIL. The API caps
    // visit descriptions at 30 chars (openapi.yml VisitFields.description maxLength: 30 ->
    // @Size(min=1,max=30) on VisitFieldsDto), and the Add Visit form was capped to match in this
    // PR, but visit-edit.component.html still carries maxlength="255". The toBeEnabled() check
    // shows the Edit form accepts a 60-char value as valid; the toHaveValue() check asserts the
    // 30-char cap that SHOULD apply here and fails until visit-edit.component.html is fixed.
    await page.goto(`${baseUrl}/pets/7/visits/add`);
    await page.locator("#description").fill("Checkup");
    await page.locator("tr").getByRole("button", { name: "Edit Visit" }).first().click();
    await page.locator("#description").fill("Full annual wellness exam with vaccines and blood panel work");
    await expect(page).toHaveURL(/\/petclinic\/visits\/\d+\/edit$/);
    await expect(page.getByRole("button", { name: "Update Visit" })).toBeEnabled();
    await expect(page.getByRole("textbox", { name: "Description" })).toHaveValue("Full annual wellness exam with");
});
