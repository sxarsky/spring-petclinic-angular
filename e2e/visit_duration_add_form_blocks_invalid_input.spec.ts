// Generated by Skyramp v1.3.40 on 2026-08-29 05:17:36.218938891 +0000 UTC m=+938.130472223
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output visit_duration_add_form_blocks_invalid_input.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/visit_duration_add_form_blocks_invalid_input_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(102000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(`${baseUrl}/pets/7/visits/add`);
    await page.locator("#durationMinutes").fill("999");
    await expect(page.getByText("Duration must be between 5")).toContainText("Duration must be between 5 and 240 minutes");
    await expect(page.locator("#durationMinutes")).toHaveClass(/is-invalid/);
    await expect(page.getByRole("button", { name: "Add Visit" })).toBeDisabled();

    await page.getByRole("spinbutton", { name: "Duration (minutes)" }).fill("240");
    await expect(page.getByRole("spinbutton", { name: "Duration (minutes)" })).toHaveValue("240");
    await expect(page.getByText("Duration must be between 5 and 240 minutes")).toHaveCount(0);
    await expect(page.locator("#durationMinutes")).toHaveClass(/is-valid/);
});
