// Generated by Skyramp v1.3.40 on 2026-08-29 09:17:26.582184093 +0000 UTC m=+549.234232331
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owners_list_pet_count_column.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owners_list_pet_count_column_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(614000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();
    await page.waitForLoadState('networkidle');
    await page.locator('#ownersTable').waitFor({ state: 'visible', timeout: 10000 });
    await expect(page).toHaveURL(/\/petclinic\/owners$/);
    await expect(page.locator("#ownersTable thead th").nth(4)).toHaveText("No. of pets");

    // Owner 1 (George Franklin) owns exactly one pet ("Leo"), as GET /petclinic/api/owners/1
    // reports. The "No. of pets" column must therefore read 1 for his row.
    const georgeFranklinRow = page
        .locator("#ownersTable tbody tr")
        .filter({ hasText: "George Franklin" });
    await expect(georgeFranklinRow.locator("td").nth(4)).toHaveText("1");
});
