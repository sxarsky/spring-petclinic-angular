// Generated by Skyramp v1.3.40 on 2026-08-30 08:46:13.374431224 +0000 UTC m=+871.746539946
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owners_name_link_navigates_to_detail.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owners_name_link_navigates_to_detail_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

// assertions reviewed: enhancement instructions target sendRequest response bodies; this is a
// pure browser test with none. Assertions were already strengthened before the enhance pass —
// the replayed heading check was joined by a toHaveURL navigation check and four exact-value
// row assertions (name, address, city, telephone) taken from GET /petclinic/api/owners/1.
// Those four are expected to FAIL: they document that OwnerDetailComponent never renders the
// fetched owner. No further assertions are needed.
test('testUi', async ({ page }) => {
    test.setTimeout(157000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();
    await expect(page.getByRole("link", { name: "George Franklin" })).toBeVisible();
    await page.getByRole("link", { name: "George Franklin" }).click();

    await expect(page).toHaveURL(/\/petclinic\/owners\/1$/);
    await expect(page.getByRole("heading", { name: "Owner Information" })).toBeVisible();

    // The owner detail page must render the owner fetched from GET /petclinic/api/owners/1.
    // OwnerDetailComponent subscribes without a markForCheck(), unlike OwnerListComponent,
    // so the fetched owner never reaches the view and these rows render empty.
    await expect(page.getByRole("row", { name: "Name" })).toContainText("George Franklin");
    await expect(page.getByRole("row", { name: "Address" })).toContainText("110 W. Liberty St.");
    await expect(page.getByRole("row", { name: "City" })).toContainText("Madison");
    await expect(page.getByRole("row", { name: "Telephone" })).toContainText("6085551023");
});
