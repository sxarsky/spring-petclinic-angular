// Generated by Skyramp v1.3.40 on 2026-08-29 07:51:34.000000000 +0000 UTC
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owner_edit_clearing_email_surfaces_error.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owner_edit_clearing_email_surfaces_error_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, getValue, newSkyrampPlaywrightPage } from '@skyramp/skyramp';
import { fillOwnerForm } from './skyrampUtils';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(103000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.getByRole("button", { name: "Owners" }).click();
    await page.getByRole("link", { name: "Search" }).click();
    await page.locator("a[href=\"/petclinic/owners/2\"]").click();
    await page.getByRole("button", { name: "Edit Owner" }).click();

    const playwrightRequest0: Record<string, any> = {
        "address": "638 Cardinal Ave.",
        "city": "Sun Prairie",
        "email": "betty.davis@@broken",
        "firstName": "Betty",
        "lastName": "Davis",
        "telephone": "6085551749"
    };

    await fillOwnerForm(
        page,
        getValue(playwrightRequest0, "firstName"),
        getValue(playwrightRequest0, "lastName"),
        getValue(playwrightRequest0, "address"),
        getValue(playwrightRequest0, "city"),
        getValue(playwrightRequest0, "telephone"),
        getValue(playwrightRequest0, "email")
    );
    // Wait to finish any potential hydration
    await page.waitForTimeout(1500);
    const responsePromise0 = page.waitForResponse("**/petclinic/api/owners/**");
    await page.getByRole("button", { name: "Update Owner" }).click();
    const response0 = await responsePromise0;

    expect(response0.status()).toBe(400);
    await expect(page.locator("app-owner-edit .alert-danger")).toBeVisible();
    await expect(page.locator("app-owner-edit .alert-danger")).toContainText("server returned code 400");
});
