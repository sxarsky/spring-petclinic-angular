// Generated by Skyramp v1.3.40 on 2026-08-29 06:01:34.627622854 +0000 UTC m=+756.119910839
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output vets-list-specialties-inline-and-empty-state.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/vets_list_specialties_inline_and_empty_state_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(60000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(`${baseUrl}/`);
    await page.getByRole("button", { name: "Veterinarians" }).click();
    await page.getByRole("link", { name: "All" }).click();
    await expect(page).toHaveURL(`${baseUrl}/petclinic/vets`);
    await expect(page.locator("#vets tbody > tr").filter({ hasText: "Linda Douglas" }).locator("td").nth(1)).toHaveText("dentistry surgery");
    await expect(page.locator("#vets tbody > tr").filter({ hasText: "Helen Leary" }).locator("td").nth(1)).toHaveText("radiology");
    await expect(page.locator("#vets tbody > tr").filter({ hasText: "James Carter" }).locator("td").nth(1)).toHaveText("No specialties");
    await expect(page.locator("#vets tbody > tr").filter({ hasText: "Linda Douglas" }).locator("td").nth(1).locator("div.d-inline")).toHaveCount(2);
    await expect(page.locator("#vets tbody > tr").filter({ hasText: "Linda Douglas" }).locator("td").nth(1).locator("span.text-muted")).toHaveCount(0);
    await expect(page.locator("#vets tbody span.text-muted")).toHaveCount(2);
});
