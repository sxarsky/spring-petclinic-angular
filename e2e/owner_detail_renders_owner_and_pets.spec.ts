// Generated by Skyramp v1.3.40 on 2026-08-29 09:20:10.828926999 +0000 UTC m=+745.459422968
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output owner_detail_renders_owner_and_pets.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/owner_detail_renders_owner_and_pets_trace.zip

// Import of required libraries
import { test, expect, getBaseUrl, newSkyrampPlaywrightPage } from '@skyramp/skyramp';
import { openOwnersList, openOwnerDetail } from './skyrampUtils';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(1076000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(baseUrl);
    await page.goto(`${baseUrl}/petclinic/owners`);
    // Wait for network activities to finish
    await page.waitForTimeout(5000);

    await page.goto(baseUrl);
    await openOwnersList(page);
    await openOwnerDetail(page);
    await expect(page.getByRole("heading", { name: "Owner Information" })).toBeVisible();
    await expect(page.getByRole("heading", { name: "Pets and visit history" })).toBeVisible();
    await expect(page).toHaveURL(`${baseUrl}/petclinic/owners/6`);
    await expect(page.locator(".ownerFullName")).toHaveText("Jean Coleman");
    await expect(page.locator("app-owner-detail")).toContainText("105 N. Lake St.");
    await expect(page.locator("app-owner-detail")).toContainText("Max");
    await expect(page.locator("app-owner-detail")).toContainText("Samantha");
});
