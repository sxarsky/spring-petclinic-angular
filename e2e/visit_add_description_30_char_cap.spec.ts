// Generated by Skyramp v1.3.40 on 2026-08-29 05:44:03.153923704 +0000 UTC m=+989.569218152
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output visit_add_description_30_char_cap.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/e2e \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/.skyramp/visit_add_description_30_char_cap_trace_1.zip

// Import of required libraries
import { test, expect, getBaseUrl, getValue, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

// URL for test requests
const baseUrl = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:4200");

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(60000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto(`${baseUrl}/pets/1/visits/add`);

    const playwrightRequest0: Record<string, any> = {"date": "2026-08-29"};

    await page.locator("#date").fill(getValue(playwrightRequest0, "date"));
    await page.locator("#description").fill("Annual rabies booster and checkup!!");
    await expect(page.getByRole("textbox", { name: "Description" })).toHaveValue("Annual rabies booster and chec");
    // Wait to finish any potential hydration
    await page.waitForTimeout(1500);
    const responsePromise0 = page.waitForResponse("**/petclinic/api/owners/**/pets/**/visits**");
    await page.getByRole("button", { name: "Add Visit" }).click();
    const response0 = await responsePromise0;

    expect(response0.status()).toBe(201);
    const createdVisit = await response0.json();
    expect(createdVisit.description).toBe("Annual rabies booster and chec");
    expect(createdVisit.description.length).toBe(30);
    expect(createdVisit.date).toBe("2026-08-29");
    expect(createdVisit.petId).toBe(1);
    expect(createdVisit.id).toBeGreaterThan(0);

    await expect(page).toHaveURL(/\/petclinic\/owners\/1$/);
});
