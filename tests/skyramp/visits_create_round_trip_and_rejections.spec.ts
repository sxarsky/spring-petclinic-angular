// Generated by Skyramp v1.3.44 on 2026-09-08 19:06:43.294873953 +0000 UTC m=+920.207028392
// Command: skyramp generate integration rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output visits_create_round_trip_and_rejections.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/tests/skyramp \
// 		--overwrite \
// 		--runtime local \
// 		--trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/tests/skyramp/scenario_visits_create_round_trip_and_rejections.json

// Import of required libraries
import { test, expect, SkyrampClient, getBaseUrl, getValue, getResponseValue } from '@skyramp/skyramp';
import { createVisit, getVisit, deleteVisit } from './skyrampUtils';

// URL for test requests
const URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");




test('testIntegration', async () => {
    test.setTimeout(60000);

    // Invocation of Skyramp Client
    const client = new SkyrampClient();
    // Request Body
    const visitsPostRequestBody: string = `{
        "date": "2026-09-08",
        "description": "skyramp regression visit",
        "petId": 1
    }`;

    const visitsPostResponse = await createVisit(client, visitsPostRequestBody);

    expect(visitsPostResponse.statusCode, 'status code').toBe(201);

    expect(getResponseValue(visitsPostResponse, "description"), 'description').toBe("skyramp regression visit");
    expect(getResponseValue(visitsPostResponse, "petId"), 'petId').toBe(1);
    expect(getResponseValue(visitsPostResponse, "date"), 'date').toBe("2026-09-08");
    expect(getResponseValue(visitsPostResponse, "id"), 'id').toBeGreaterThan(0);
    expect(getResponseValue(visitsPostResponse, "createdAt"), 'createdAt is not exposed by the Visit schema').toBeNull();

    const visitsVisitsGetResponse = await getVisit(client, getResponseValue(visitsPostResponse, "id"));

    expect(visitsVisitsGetResponse.statusCode, 'status code').toBe(200);

    expect(getResponseValue(visitsVisitsGetResponse, "id"), 'id').toBe(getResponseValue(visitsPostResponse, "id"));
    expect(getResponseValue(visitsVisitsGetResponse, "description"), 'description').toBe("skyramp regression visit");
    expect(getResponseValue(visitsVisitsGetResponse, "petId"), 'petId').toBe(1);
    expect(getResponseValue(visitsVisitsGetResponse, "date"), 'date').toBe("2026-09-08");
    expect(getResponseValue(visitsVisitsGetResponse, "createdAt"), 'createdAt is not exposed by the Visit schema').toBeNull();

    // Request Body
    const visitsPost1RequestBody: string = `{
        "date": "2026-09-08",
        "petId": 1
    }`;

    const visitsPost1Response = await createVisit(client, visitsPost1RequestBody, {
        "date": getResponseValue(visitsVisitsGetResponse, "date"),
        "petId": getResponseValue(visitsVisitsGetResponse, "petId")
    }, 400);

    expect(visitsPost1Response.statusCode, 'status code').toBe(400);

    expect(getResponseValue(visitsPost1Response, "title"), 'title').toBe("MethodArgumentNotValidException");
    expect(getResponseValue(visitsPost1Response, "status"), 'status').toBe(400);
    expect(getResponseValue(visitsPost1Response, "detail"), 'detail').toBe("The request contains invalid or missing parameters");

    // Request Body
    const visitsPost2RequestBody: string = `{
        "date": "2026-09-08",
        "description": "skyramp orphan visit",
        "petId": 999999
    }`;

    const visitsPost2Response = await createVisit(client, visitsPost2RequestBody, {
        "date": getResponseValue(visitsVisitsGetResponse, "date")
    }, 404);

    expect(visitsPost2Response.statusCode, 'status code').toBe(404);

    expect(getResponseValue(visitsPost2Response, "title"), 'title').toBe("DataIntegrityViolationException");
    expect(getResponseValue(visitsPost2Response, "status"), 'status').toBe(404);
    expect(getResponseValue(visitsPost2Response, "detail"), 'detail').toBe("The requested resource could not be processed due to a data constraint violation");

    const visitsVisitsDeleteResponse = await deleteVisit(client, getResponseValue(visitsPostResponse, "id"));

    expect(visitsVisitsDeleteResponse.statusCode, 'status code').toBe(204);

    const visitsVisitsGet1Response = await getVisit(client, getResponseValue(visitsPostResponse, "id"), 404);

    expect(visitsVisitsGet1Response.statusCode, 'status code').toBe(404);

    expect(getResponseValue(visitsVisitsGet1Response, "id"), 'deleted visit returns no body').toBeNull();
    expect(getResponseValue(visitsVisitsGet1Response, "description"), 'deleted visit returns no body').toBeNull();
});
