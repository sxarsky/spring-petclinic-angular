// Generated by Skyramp v1.3.44 on 2026-09-09 04:57:41.196666692 +0000 UTC m=+2052.383895392
// Command: skyramp generate integration rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output visit_duration_create_read_update_roundtrip_integration_test.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/tests/skyramp \
// 		--overwrite \
// 		--runtime local \
// 		--trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/tests/skyramp/scenario_visit_duration_create_read_update_roundtrip.json

// Import of required libraries
import { test, expect, SkyrampClient, getBaseUrl, getValue, getResponseValue } from '@skyramp/skyramp';
import { createOwner, createPet, createVisit, updateVisit, getVisit, deleteOwner } from './skyrampUtils';

// URL for test requests
const URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

test('testIntegration', async () => {
    test.setTimeout(60000);

    // Invocation of Skyramp Client
    const client = new SkyrampClient();

    // Request Body
    const ownersPostRequestBody: string = `{
        "address": "2 Test St.",
        "city": "Madison",
        "firstName": "Skyramp",
        "lastName": "Roundtrip",
        "telephone": "6085550002"
    }`;

    const ownersPostResponse = await createOwner(client, URL_localhost, ownersPostRequestBody);

    try {

        expect(ownersPostResponse.statusCode, 'status code').toBe(201);
        expect(getResponseValue(ownersPostResponse, "firstName"), 'firstName').toBe("Skyramp");
        expect(getResponseValue(ownersPostResponse, "lastName"), 'lastName').toBe("Roundtrip");
        expect(getResponseValue(ownersPostResponse, "address"), 'address').toBe("2 Test St.");
        expect(getResponseValue(ownersPostResponse, "city"), 'city').toBe("Madison");
        expect(getResponseValue(ownersPostResponse, "telephone"), 'telephone').toBe("6085550002");
        expect(getResponseValue(ownersPostResponse, "id"), 'id').toBeGreaterThan(0);

        // Request Body
        const petsPostRequestBody: string = `{
            "birthDate": "2020-01-01",
            "name": "Roundtrip",
            "type": {
                "id": 1,
                "name": "cat"
            }
        }`;

        const petsPostResponse = await createPet(
            client,
            URL_localhost,
            getResponseValue(ownersPostResponse, "id"),
            petsPostRequestBody
        );

        expect(petsPostResponse.statusCode, 'status code').toBe(201);
        expect(getResponseValue(petsPostResponse, "name"), 'name').toBe("Roundtrip");
        expect(getResponseValue(petsPostResponse, "birthDate"), 'birthDate').toBe("2020-01-01");
        expect(getResponseValue(petsPostResponse, "type.name"), 'type.name').toBe("cat");
        expect(getResponseValue(petsPostResponse, "ownerId"), 'ownerId').toBe(getResponseValue(ownersPostResponse, "id"));
        expect(getResponseValue(petsPostResponse, "id"), 'id').toBeGreaterThan(0);

        // Request Body
        const visitsPostRequestBody: string = `{
            "date": "2026-04-01",
            "description": "explicit duration",
            "durationMinutes": 60
        }`;

        const visitsPostResponse = await createVisit(
            client,
            URL_localhost,
            getResponseValue(ownersPostResponse, "id"),
            getResponseValue(petsPostResponse, "id"),
            visitsPostRequestBody
        );

        expect(visitsPostResponse.statusCode, 'status code').toBe(201);
        expect(getResponseValue(visitsPostResponse, "date"), 'date').toBe("2026-04-01");
        expect(getResponseValue(visitsPostResponse, "description"), 'description').toBe("explicit duration");
        expect(getResponseValue(visitsPostResponse, "durationMinutes"), 'durationMinutes').toBe(60);
        expect(getResponseValue(visitsPostResponse, "petId"), 'petId').toBe(getResponseValue(petsPostResponse, "id"));
        expect(getResponseValue(visitsPostResponse, "id"), 'id').toBeGreaterThan(0);

        // Request Body
        const visitsPost1RequestBody: string = `{
            "date": "2026-04-02",
            "description": "defaulted duration"
        }`;

        const visitsPost1Response = await createVisit(
            client,
            URL_localhost,
            getResponseValue(ownersPostResponse, "id"),
            getResponseValue(petsPostResponse, "id"),
            visitsPost1RequestBody
        );

        expect(visitsPost1Response.statusCode, 'status code').toBe(201);
        expect(getResponseValue(visitsPost1Response, "date"), 'date').toBe("2026-04-02");
        expect(getResponseValue(visitsPost1Response, "description"), 'description').toBe("defaulted duration");
        // durationMinutes was omitted from the request, so the schema default of 30 applies.
        expect(getResponseValue(visitsPost1Response, "durationMinutes"), 'durationMinutes').toBe(30);
        expect(getResponseValue(visitsPost1Response, "petId"), 'petId').toBe(getResponseValue(petsPostResponse, "id"));

        // Request Body
        const visitsVisitsPutRequestBody: string = `{
            "date": "2026-04-01",
            "description": "explicit duration",
            "durationMinutes": 45
        }`;

        const visitsVisitsPutResponse = await updateVisit(
            client,
            URL_localhost,
            getResponseValue(visitsPostResponse, "id"),
            visitsVisitsPutRequestBody
        );

        expect(visitsVisitsPutResponse.statusCode, 'status code').toBe(204);

        const visitsVisitsGetResponse = await getVisit(
            client,
            URL_localhost,
            getResponseValue(visitsPostResponse, "id")
        );

        expect(visitsVisitsGetResponse.statusCode, 'status code').toBe(200);
        expect(getResponseValue(visitsVisitsGetResponse, "durationMinutes"), 'durationMinutes').toBe(45);
        expect(getResponseValue(visitsVisitsGetResponse, "description"), 'description').toBe("explicit duration");
        expect(getResponseValue(visitsVisitsGetResponse, "date"), 'date').toBe("2026-04-01");
        expect(getResponseValue(visitsVisitsGetResponse, "id"), 'id').toBe(getResponseValue(visitsPostResponse, "id"));

        const visits99999999GetResponse = await getVisit(client, URL_localhost, "99999999", 404);

        expect(visits99999999GetResponse.statusCode, 'status code').toBe(404);
        expect(getResponseValue(visits99999999GetResponse, "id"), 'id').toBeNull();
        expect(getResponseValue(visits99999999GetResponse, "durationMinutes"), 'durationMinutes').toBeNull();
    } finally {
        const ownersOwnersDeleteResponse = await deleteOwner(
            client,
            URL_localhost,
            getResponseValue(ownersPostResponse, "id")
        );

        expect(ownersOwnersDeleteResponse.statusCode, 'status code').toBe(204);
    }
});
