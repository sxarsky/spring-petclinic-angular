// Generated by Skyramp v1.3.40 on 2026-09-02 07:54:34.356675008 +0000 UTC m=+569.978911227
// Command: skyramp generate contract rest http://localhost:9966/petclinic/api/owners \
// 		--framework playwright \
// 		--language typescript \
// 		--method GET \
// 		--output contract_owners_sort_by_city.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/tests/skyramp \
// 		--overwrite \
// 		--provider \
// 		--query-params sort=city \
// 		--response-status-code 200 \
// 		--runtime local \
// 		--skipProvisionParents

// Import of required libraries
import { test, expect, SkyrampClient, getBaseUrl, getValue } from '@skyramp/skyramp';

// URL for test requests
const URL = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

// contract test for /petclinic/api/owners GET
test('testOwnersGet', async () => {
    // Invocation of Skyramp Client
    const client = new SkyrampClient();
    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["Authorization"] = "Bearer " + process.env.SKYRAMP_TEST_TOKEN;
    }

    // Execute Request
    const ownersGetResponse = await client.sendRequest({
        url: URL,
        path: "/petclinic/api/owners",
        method: "GET",
        headers: headers,
        queryParams: {"sort": "city"}
    });

    // Generated Assertions
    expect(ownersGetResponse.statusCode, 'status code').toBe(200);

    const cities: string[] = [];
    for (let i = 0; ; i++) {
        const city = getValue(ownersGetResponse, `${i}.city`);
        if (city === null || city === undefined) {
            break;
        }
        cities.push(city as string);
    }

    expect(cities.length, 'owners returned').toBeGreaterThan(1);
    expect(cities, 'cities ordered ascending by city').toEqual([...cities].sort((a, b) => (a < b ? -1 : a > b ? 1 : 0)));
});
