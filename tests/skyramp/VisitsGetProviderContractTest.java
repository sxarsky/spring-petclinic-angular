// Generated by Skyramp v1.3.40 on 2026-09-02 07:55:41.483729475 +0000 UTC m=+638.551811443
// Command: skyramp generate contract rest http://localhost:9966/petclinic/api/visits \
// 		--framework junit \
// 		--language java \
// 		--method GET \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/tests/skyramp \
// 		--overwrite \
// 		--provider \
// 		--provider-output VisitsListCreatedAtContractTest.java \
// 		--response-data [{"id":1,"petId":7,"date":"2013-01-01","description":"rabies shot","createdAt":"2026-09-02T07:48:00"}] \
// 		--response-status-code 200 \
// 		--runtime local \
// 		--skipProvisionParents

import java.util.*;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

import static dev.skyramp.api.Skyramp.*;
import dev.skyramp.api.SkyrampClient;
import dev.skyramp.exception.SkyrampException;
import dev.skyramp.model.*;

public final class VisitsGetProviderContractTest {
    // URL for test requests
    String URL = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

    // contract test for /petclinic/api/visits GET
    @Test
    public void testVisitsGet() throws SkyrampException {
        // Invocation of Skyramp Client
        var client = new SkyrampClient();
        // Definition of authentication header
        String token = System.getenv("SKYRAMP_TEST_TOKEN");
        Map<String, String> headers = Map.of();
        if (token != null && !token.isEmpty()) {
            headers = Map.of("Authorization", "Bearer " + token);
        }

        // Expected Response Body
        String expectedVisitsGetResponseBody = """
            [
                {
                    "createdAt": "2026-09-02T07:48:00",
                    "date": "2013-01-01",
                    "description": "rabies shot",
                    "id": 1,
                    "petId": 7
                }
            ]
        """;

        // Execute Request
        var visitsGetResponse = client.sendRequest(RequestV2.builder()
            .url(URL)
            .path("/petclinic/api/visits")
            .method("GET")
            .headers(headers)
            .build());

        // Generated Assertions
        assertTrue(checkStatusCode(visitsGetResponse, "200"));
        assertTrue(checkSchema(visitsGetResponse, expectedVisitsGetResponseBody));
        assertNotNull(getValue(visitsGetResponse, "0.createdAt"));
        assertNotNull(getValue(visitsGetResponse, "0.date"));
        assertNotNull(getValue(visitsGetResponse, "0.description"));
        assertNotNull(getValue(visitsGetResponse, "0.id"));
        assertNotNull(getValue(visitsGetResponse, "0.petId"));
        assertTrue(Double.parseDouble(String.valueOf(getValue(visitsGetResponse, "0.id"))) > 0);
        assertTrue(Double.parseDouble(String.valueOf(getValue(visitsGetResponse, "0.petId"))) > 0);
        assertTrue(String.valueOf(getValue(visitsGetResponse, "0.date")).matches("^\\d{4}-\\d{2}-\\d{2}$"));
        assertTrue(String.valueOf(getValue(visitsGetResponse, "0.createdAt")).matches("^\\d{4}-\\d{2}-\\d{2}T.*"));
    }
}
