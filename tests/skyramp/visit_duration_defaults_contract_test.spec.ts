// Generated by Skyramp v1.3.40 on 2026-09-02 08:09:25.244015906 +0000 UTC m=+1445.518960789
// Command: skyramp generate contract rest http://localhost:9966/petclinic/api/owners/{ownerId}/pets/{petId}/visits \
// 		--api-schema /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-rest/src/main/resources/openapi.yml \
// 		--framework playwright \
// 		--language typescript \
// 		--method POST \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/tests/skyramp \
// 		--overwrite \
// 		--path-params ownerId=1,petId=1 \
// 		--provider \
// 		--provider-output visit_duration_defaults_contract_test.spec.ts \
// 		--request-data {"date":"2026-09-04","description":"default length visit"} \
// 		--response-data {"id":1,"petId":1,"date":"2026-09-04","description":"default length visit","durationMinutes":30} \
// 		--response-status-code 201 \
// 		--runtime local \
// 		--skipProvisionParents

// Import of required libraries
import { test, expect, SkyrampClient, checkSchema, getBaseUrl, getValue, getResponseValue } from '@skyramp/skyramp';

// URL for test requests
const URL = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

// contract test for /petclinic/api/owners/{ownerId}/pets/{petId}/visits POST
test('testVisitsPost', async () => {
    // Invocation of Skyramp Client
    const client = new SkyrampClient();
    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["Authorization"] = "Bearer " + process.env.SKYRAMP_TEST_TOKEN;
    }

    // Declaration of variables
    const ownerId = "1";
    const petId = "1";

    // Request Body
    const visitsPostRequestBody: string = `{
        "date": "2026-09-04",
        "description": "default length visit"
    }`;

    // Expected Response Body
    const expectedVisitsPostResponseBody: string = `{
        "date": "2026-09-04",
        "description": "default length visit",
        "durationMinutes": 30,
        "id": 1,
        "petId": 1
    }`;

    // Execute Request
    const visitsPostResponse = await client.sendRequest({
        url: URL,
        path: "/petclinic/api/owners/{ownerId}/pets/{petId}/visits",
        method: "POST",
        body: visitsPostRequestBody,
        headers: headers,
        pathParams: {
            "ownerId": ownerId,
            "petId": petId
        }
    });

    // Generated Assertions
    expect(visitsPostResponse.statusCode, 'status code').toBe(201);
    expect(checkSchema(visitsPostResponse, expectedVisitsPostResponseBody), 'schema').toBeTruthy();
    expect(getResponseValue(visitsPostResponse, "date"), 'value').not.toBeNull();
    expect(getResponseValue(visitsPostResponse, "description"), 'value').not.toBeNull();
    expect(getResponseValue(visitsPostResponse, "durationMinutes"), 'value').not.toBeNull();
    expect(getResponseValue(visitsPostResponse, "date"), 'date').toBe("2026-09-04");
    expect(getResponseValue(visitsPostResponse, "description"), 'description').toBe("default length visit");
    // durationMinutes was omitted from the request body, so the schema default of 30 must be applied
    expect(getResponseValue(visitsPostResponse, "durationMinutes"), 'durationMinutes').toBe(30);
    expect(getResponseValue(visitsPostResponse, "durationMinutes"), 'durationMinutes at least the 5 minute minimum').toBeGreaterThanOrEqual(5);
    expect(getResponseValue(visitsPostResponse, "durationMinutes"), 'durationMinutes at most the 240 minute maximum').toBeLessThanOrEqual(240);
    expect(getResponseValue(visitsPostResponse, "petId"), 'petId').toBe(Number(petId));
    expect(getResponseValue(visitsPostResponse, "id"), 'id').toBeGreaterThan(0);
});
