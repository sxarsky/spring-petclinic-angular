// Generated by Skyramp v1.3.40 on 2026-08-29 09:28:33.438401063 +0000 UTC m=+1233.568185056
// Command: skyramp generate contract rest http://localhost:9966/petclinic/api/visits \
// 		--framework playwright \
// 		--language typescript \
// 		--method POST \
// 		--output contract_visit_duration_validation_bounds.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/tests/skyramp \
// 		--overwrite \
// 		--provider \
// 		--request-data {"petId":7,"date":"2026-08-03","description":"at max duration","durationMinutes":240} \
// 		--response-data {"id":9004,"petId":7,"date":"2026-08-03","description":"at max duration","durationMinutes":240} \
// 		--response-status-code 201 \
// 		--runtime local \
// 		--skipProvisionParents

// Import of required libraries
import { test, expect, SkyrampClient, checkSchema, getBaseUrl, getResponseValue } from '@skyramp/skyramp';

// URL for test requests
const URL = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

// contract test for /petclinic/api/visits POST
test('testVisitsPost', async () => {
    // Invocation of Skyramp Client
    const client = new SkyrampClient();
    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["Authorization"] = "Bearer " + process.env.SKYRAMP_TEST_TOKEN;
    }

    // Request Body
    const visitsPostRequestBody: string = `{
        "date": "2026-08-03",
        "description": "at max duration",
        "durationMinutes": 240,
        "petId": 7
    }`;

    // Expected Response Body
    const expectedVisitsPostResponseBody: string = `{
        "date": "2026-08-03",
        "description": "at max duration",
        "durationMinutes": 240,
        "id": 9004,
        "petId": 7
    }`;

    // Execute Request
    const visitsPostResponse = await client.sendRequest({
        url: URL,
        path: "/petclinic/api/visits",
        method: "POST",
        body: visitsPostRequestBody,
        headers: headers
    });

    // Generated Assertions
    expect(visitsPostResponse.statusCode, 'status code').toBe(201);
    expect(checkSchema(visitsPostResponse, expectedVisitsPostResponseBody), 'schema').toBeTruthy();
    expect(getResponseValue(visitsPostResponse, "date"), 'date').toBe("2026-08-03");
    expect(getResponseValue(visitsPostResponse, "description"), 'description').toBe("at max duration");
    expect(getResponseValue(visitsPostResponse, "petId"), 'petId').toBe(7);
    expect(getResponseValue(visitsPostResponse, "id"), 'id').toBeGreaterThan(0);
    // 240 is the inclusive upper bound declared by openapi.yml (maximum: 240):
    // it must be accepted and echoed back unchanged.
    expect(getResponseValue(visitsPostResponse, "durationMinutes"), 'durationMinutes').toBe(240);
});
