// Generated by Skyramp on 2026-09-02 08:08:55.211 Coordinated Universal Time


import { expect, SkyrampClient } from '@skyramp/skyramp';

export async function createOwner(client: SkyrampClient, url: string, body: string, expectedStatus = 201) {
    const response = await client.sendRequest({
        url: url,
        path: "/petclinic/api/owners",
        method: "POST",
        body: body
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function createOwnerPet(client: SkyrampClient, url: string, ownerId: string | number, body: string, expectedStatus = 201) {
    const response = await client.sendRequest({
        url: url,
        path: "/petclinic/api/owners/{owners}/pets",
        method: "POST",
        body: body,
        pathParams: {"owners": ownerId}
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function createOwnerPetVisit(client: SkyrampClient, url: string, ownerId: string | number, petId: string | number, body: string, expectedStatus = 201) {
    const response = await client.sendRequest({
        url: url,
        path: "/petclinic/api/owners/{owners}/pets/{pets}/visits",
        method: "POST",
        body: body,
        pathParams: {
            "owners": ownerId,
            "pets": petId
        }
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function deleteOwnerPets(client: SkyrampClient, url: string, ownerId: string | number, expectedStatus = 204) {
    const response = await client.sendRequest({
        url: url,
        path: "/petclinic/api/owners/{owners}/pets",
        method: "DELETE",
        pathParams: {"owners": ownerId}
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function getOwner(client: SkyrampClient, url: string, ownerId: string | number, expectedStatus = 200) {
    const response = await client.sendRequest({
        url: url,
        path: "/petclinic/api/owners/{owners}",
        method: "GET",
        pathParams: {"owners": ownerId}
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}
