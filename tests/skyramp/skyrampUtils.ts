// Generated by Skyramp on 2026-08-29 09:29:35.852 Coordinated Universal Time


import { expect } from '@skyramp/skyramp';

export async function createVisit(client, url, date, description, durationMinutes, petId, expectedStatus = 201) {
    const visitsPostRequestBody: string = `{
        "date": "${date}",
        "description": "${description}",
        "durationMinutes": ${durationMinutes},
        "petId": ${petId}
    }`;

    const visitsPostResponse = await client.sendRequest({
        url: url,
        path: "/petclinic/api/visits",
        method: "POST",
        body: visitsPostRequestBody
    });

    expect(visitsPostResponse.statusCode, 'status code').toBe(expectedStatus);
    return visitsPostResponse;
}

export async function updateVisit(client, url, visitId, date, description, durationMinutes, expectedStatus = 204) {
    const visitsVisitsPutRequestBody: string = `{
        "date": "${date}",
        "description": "${description}",
        "durationMinutes": ${durationMinutes}
    }`;

    const visitsVisitsPutResponse = await client.sendRequest({
        url: url,
        path: "/petclinic/api/visits/{visits}",
        method: "PUT",
        body: visitsVisitsPutRequestBody,
        pathParams: {"visits": visitId}
    });

    expect(visitsVisitsPutResponse.statusCode, 'status code').toBe(expectedStatus);
    return visitsVisitsPutResponse;
}

export async function getVisit(client, url, visitId, expectedStatus = 200) {
    const visitsVisitsGetResponse = await client.sendRequest({
        url: url,
        path: "/petclinic/api/visits/{visits}",
        method: "GET",
        pathParams: {"visits": visitId}
    });

    expect(visitsVisitsGetResponse.statusCode, 'status code').toBe(expectedStatus);
    return visitsVisitsGetResponse;
}

export async function getOwner(client, url, ownerId, expectedStatus = 200) {
    const ownersGetResponse = await client.sendRequest({
        url: url,
        path: "/petclinic/api/owners/{owners}",
        method: "GET",
        pathParams: {"owners": ownerId}
    });

    expect(ownersGetResponse.statusCode, 'status code').toBe(expectedStatus);
    return ownersGetResponse;
}
