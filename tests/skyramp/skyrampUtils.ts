// Generated by Skyramp on 2026-08-29 09:26:42.753 Coordinated Universal Time


import { expect, getBaseUrl } from '@skyramp/skyramp';

// URL for test requests
const URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

export async function createVisit(client, date, description, petId, expectedStatus = 201) {
    // Request Body
    const visitsPostRequestBody: string = `{
        "date": "2026-08-29",
        "description": "123456789012345678901234567890",
        "petId": 1
    }`;

    // Execute Request
    const visitsPostResponse = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/visits",
        method: "POST",
        body: visitsPostRequestBody,
        dataOverride: {
            "date": date,
            "description": description,
            "petId": petId
        }
    });

    // Generated Assertions
    expect(visitsPostResponse.statusCode, 'status code').toBe(expectedStatus);

    return visitsPostResponse;
}

export async function getVisit(client, visitId, expectedStatus = 200) {
    // Execute Request
    const visitsVisitsGetResponse = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/visits/{visits}",
        method: "GET",
        pathParams: {"visits": visitId}
    });

    // Generated Assertions
    expect(visitsVisitsGetResponse.statusCode, 'status code').toBe(expectedStatus);

    return visitsVisitsGetResponse;
}
