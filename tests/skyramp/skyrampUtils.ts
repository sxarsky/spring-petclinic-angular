// Generated by Skyramp on 2026-09-08 19:23:51.396 Coordinated Universal Time

import { expect, SkyrampClient, getBaseUrl } from '@skyramp/skyramp';
// URL for test requests
export const URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

export async function createPetType(client: SkyrampClient, name: string, expectedStatus = 201) {
    // Request Body
    const pettypesPostRequestBody: string = `{
        "name": "skyramp-count-type"
    }`;

    // Execute Request
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/pettypes",
        method: "POST",
        body: pettypesPostRequestBody,
        dataOverride: {"name": name}
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function getPetTypesList(client: SkyrampClient, expectedStatus = 200) {
    // Execute Request
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/pettypes",
        method: "GET"
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function createOwner(
    client: SkyrampClient,
    firstName: string,
    lastName: string,
    address: string,
    city: string,
    telephone: string,
    expectedStatus = 201
) {
    // Request Body
    const ownersPostRequestBody: string = `{
        "address": "1 Collection Way",
        "city": "Madison",
        "firstName": "Petcount",
        "lastName": "Skyrampcount",
        "telephone": "6085550111"
    }`;

    // Execute Request
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners",
        method: "POST",
        body: ownersPostRequestBody,
        dataOverride: {
            "firstName": firstName,
            "lastName": lastName,
            "address": address,
            "city": city,
            "telephone": telephone
        }
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function createPetForOwner(
    client: SkyrampClient,
    ownerId: string,
    petName: string,
    birthDate: string,
    petTypeId: string,
    petTypeName: string,
    expectedStatus = 201
) {
    // Request Body
    const petsPostRequestBody: string = `{
        "birthDate": "2020-05-01",
        "name": "Countable",
        "type": {
            "id": 7001,
            "name": "skyramp-count-type"
        }
    }`;

    // Execute Request
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners/{owners}/pets",
        method: "POST",
        body: petsPostRequestBody,
        pathParams: {"owners": ownerId},
        dataOverride: {
            "name": petName,
            "birthDate": birthDate,
            "type.id": petTypeId,
            "type.name": petTypeName
        }
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function getOwner(client: SkyrampClient, ownerId: string, expectedStatus = 200) {
    // Execute Request
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners/{owners}",
        method: "GET",
        pathParams: {"owners": ownerId}
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function getOwnersList(
    client: SkyrampClient,
    queryParams: Record<string, string[]> = {},
    expectedStatus = 200
) {
    // Execute Request
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners",
        method: "GET",
        queryParams: queryParams
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function deleteOwner(client: SkyrampClient, ownerId: string, expectedStatus = 204) {
    // Execute Request
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners/{owners}",
        method: "DELETE",
        pathParams: {"owners": ownerId}
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}
