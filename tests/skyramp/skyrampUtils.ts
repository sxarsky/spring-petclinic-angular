// Generated by Skyramp on 2026-09-08 19:15:51.766 Coordinated Universal Time


import { expect, SkyrampClient, getBaseUrl } from '@skyramp/skyramp';

// URL for test requests
export const URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

export async function getOwnersList(client: SkyrampClient, queryParams: Record<string, string[]> = {}, expectedStatus = 200) {
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners",
        method: "GET",
        queryParams: queryParams
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function createOwner(client: SkyrampClient, city = "Skyramp Boundary City xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx", lastName = "Maxlen", telephone = "6085550120", expectedStatus = 201) {
    // Request Body
    const ownersPostRequestBody: string = `{
        "address": "1 Boundary St",
        "city": "Skyramp Boundary City xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
        "firstName": "Cityboundary",
        "lastName": "Maxlen",
        "telephone": "6085550120"
    }`;

    // Execute Request
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners",
        method: "POST",
        body: ownersPostRequestBody,
        dataOverride: {
            "city": city,
            "lastName": lastName,
            "telephone": telephone
        }
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function getOwner(client: SkyrampClient, ownerId: any, expectedStatus = 200) {
    // Execute Request
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners/{owners}",
        method: "GET",
        pathParams: {"owners": ownerId}
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function deleteOwner(client: SkyrampClient, ownerId: any, expectedStatus = 204) {
    // Execute Request
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners/{owners}",
        method: "DELETE",
        pathParams: {"owners": ownerId}
    });

    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}
