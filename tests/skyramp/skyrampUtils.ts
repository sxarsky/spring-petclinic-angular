// Generated by Skyramp on 2026-09-08 19:15:36.514 Coordinated Universal Time


import { expect, SkyrampClient, getBaseUrl } from '@skyramp/skyramp';

// URL for test requests
const URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

export async function createOwner(client: SkyrampClient, body: string, expectedStatus = 201, dataOverride: Record<string, any> = {}) {
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners",
        method: "POST",
        body: body,
        dataOverride: dataOverride
    });
    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function getOwner(client: SkyrampClient, ownerId: number, expectedStatus = 200) {
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners/{owners}",
        method: "GET",
        pathParams: {"owners": ownerId}
    });
    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function deleteOwner(client: SkyrampClient, ownerId: number, expectedStatus = 204) {
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners/{owners}",
        method: "DELETE",
        pathParams: {"owners": ownerId}
    });
    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}
