// Generated by Skyramp on 2026-09-02 08:13:57.128 Coordinated Universal Time

import { expect, SkyrampClient, getBaseUrl } from '@skyramp/skyramp';

// URL for test requests
const URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

export async function createOwner(client: SkyrampClient, phoneNumber: string, expectedStatus = 201) {
    // Request Body
    const ownersPostRequestBody: string = `{
        "address": "9 Radium St.",
        "city": "Madison",
        "firstName": "Marie",
        "lastName": "Curie",
        "phoneNumber": "6085551111"
    }`;

    // Execute Request
    const ownersPostResponse = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners",
        method: "POST",
        body: ownersPostRequestBody,
        dataOverride: {
            "phoneNumber": phoneNumber
        }
    });

    // Generated Assertions
    expect(ownersPostResponse.statusCode, 'status code').toBe(expectedStatus);
    return ownersPostResponse;
}

export async function getOwner(client: SkyrampClient, ownerId: string | number, expectedStatus = 200) {
    // Execute Request
    const ownersOwnersGetResponse = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners/{owners}",
        method: "GET",
        pathParams: {"owners": ownerId}
    });

    // Generated Assertions
    expect(ownersOwnersGetResponse.statusCode, 'status code').toBe(expectedStatus);
    return ownersOwnersGetResponse;
}

export async function updateOwner(client: SkyrampClient, ownerId: string | number, phoneNumber: string, address: string, city: string, firstName: string, lastName: string, expectedStatus = 204) {
    // Request Body
    const ownersOwnersPutRequestBody: string = `{
        "address": "9 Radium St.",
        "city": "Madison",
        "firstName": "Marie",
        "lastName": "Curie",
        "phoneNumber": "6085552222"
    }`;

    // Execute Request
    const ownersOwnersPutResponse = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners/{owners}",
        method: "PUT",
        body: ownersOwnersPutRequestBody,
        pathParams: {"owners": ownerId},
        dataOverride: {
            "address": address,
            "city": city,
            "firstName": firstName,
            "lastName": lastName,
            "phoneNumber": phoneNumber
        }
    });

    // Generated Assertions
    expect(ownersOwnersPutResponse.statusCode, 'status code').toBe(expectedStatus);
    return ownersOwnersPutResponse;
}
