// Generated by Skyramp on 2026-09-02 08:12:20.443 Coordinated Universal Time


import { expect, SkyrampClient, getBaseUrl } from '@skyramp/skyramp';

// URL for test requests
export const URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

export async function createOwner(
    client: SkyrampClient,
    firstName: string,
    lastName: string,
    address: string,
    city: string,
    telephone: string,
    expectedStatus = 201
) {
    const ownersPostRequestBody: string = `{
        "address": "${address}",
        "city": "${city}",
        "firstName": "${firstName}",
        "lastName": "${lastName}",
        "telephone": "${telephone}"
    }`;

    const ownersPostResponse = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners",
        method: "POST",
        body: ownersPostRequestBody
    });

    expect(ownersPostResponse.statusCode, 'status code').toBe(expectedStatus);
    return ownersPostResponse;
}

export async function createPet(
    client: SkyrampClient,
    ownerId: unknown,
    name: string,
    birthDate: string,
    petTypeId: number,
    petTypeName: string,
    expectedStatus = 201
) {
    const petsPostRequestBody: string = `{
        "birthDate": "${birthDate}",
        "name": "${name}",
        "type": {
            "id": ${petTypeId},
            "name": "${petTypeName}"
        }
    }`;

    const petsPostResponse = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners/{owners}/pets",
        method: "POST",
        body: petsPostRequestBody,
        pathParams: {"owners": ownerId}
    });

    expect(petsPostResponse.statusCode, 'status code').toBe(expectedStatus);
    return petsPostResponse;
}

export async function createVisit(
    client: SkyrampClient,
    ownerId: unknown,
    petId: unknown,
    date: string,
    description: string,
    durationMinutes: number,
    expectedStatus = 201
) {
    const visitsPostRequestBody: string = `{
        "date": "${date}",
        "description": "${description}",
        "durationMinutes": ${durationMinutes}
    }`;

    const visitsPostResponse = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/owners/{owners}/pets/{pets}/visits",
        method: "POST",
        body: visitsPostRequestBody,
        pathParams: {
            "owners": ownerId,
            "pets": petId
        }
    });

    expect(visitsPostResponse.statusCode, 'status code').toBe(expectedStatus);
    return visitsPostResponse;
}

export async function updateVisit(
    client: SkyrampClient,
    visitId: unknown,
    date: string,
    description: string,
    durationMinutes: number,
    expectedStatus = 204
) {
    const visitsVisitsPutRequestBody: string = `{
        "date": "${date}",
        "description": "${description}",
        "durationMinutes": ${durationMinutes}
    }`;

    const visitsVisitsPutResponse = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/visits/{visits}",
        method: "PUT",
        body: visitsVisitsPutRequestBody,
        pathParams: {"visits": visitId}
    });

    expect(visitsVisitsPutResponse.statusCode, 'status code').toBe(expectedStatus);
    return visitsVisitsPutResponse;
}

export async function getVisit(client: SkyrampClient, visitId: unknown, expectedStatus = 200) {
    const visitsVisitsGetResponse = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/visits/{visits}",
        method: "GET",
        pathParams: {"visits": visitId}
    });

    expect(visitsVisitsGetResponse.statusCode, 'status code').toBe(expectedStatus);
    return visitsVisitsGetResponse;
}

export async function getPet(client: SkyrampClient, petId: unknown, expectedStatus = 200) {
    const petsPetsGetResponse = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/pets/{pets}",
        method: "GET",
        pathParams: {"pets": petId}
    });

    expect(petsPetsGetResponse.statusCode, 'status code').toBe(expectedStatus);
    return petsPetsGetResponse;
}
