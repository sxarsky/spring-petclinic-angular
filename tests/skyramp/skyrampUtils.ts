// Generated by Skyramp on 2026-08-30 08:54:43.942 Coordinated Universal Time


import { expect, getBaseUrl } from '@skyramp/skyramp';

// URL for test requests
const URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

export async function createVisit(client, ownerId, petId, body, expectedStatus = 201) {
    const response = await client.sendRequest({
        url: URL_localhost,
        path: `/petclinic/api/owners/${ownerId}/pets/${petId}/visits`,
        method: "POST",
        body: body
    });
    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function updateVisit(client, visitId, body, expectedStatus = 204) {
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/visits/{visits}",
        method: "PUT",
        body: body,
        pathParams: {"visits": visitId}
    });
    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}

export async function getVisit(client, visitId, expectedStatus = 200) {
    const response = await client.sendRequest({
        url: URL_localhost,
        path: "/petclinic/api/visits/{visits}",
        method: "GET",
        pathParams: {"visits": visitId}
    });
    expect(response.statusCode, 'status code').toBe(expectedStatus);
    return response;
}
