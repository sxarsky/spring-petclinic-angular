// Generated by Skyramp v1.3.44 on 2026-09-09 04:57:44.883376735 +0000 UTC m=+2056.070605435
// Command: skyramp generate integration rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output visit_duration_range_rejected_integration_test.spec.ts \
// 		--output-dir /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/tests/skyramp \
// 		--overwrite \
// 		--runtime local \
// 		--trace /home/runner/work/spring-petclinic-angular/spring-petclinic-angular/spring-petclinic-angular/tests/skyramp/scenario_visit_duration_range_rejected.json

// Import of required libraries
import { test, expect, SkyrampClient, getBaseUrl, getValue, getResponseValue } from '@skyramp/skyramp';
import { createOwner, createPet, createVisit, updateVisit, getVisit, deleteOwner } from './skyrampUtils';

// URL for test requests
const URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:9966");

test('testIntegration', async () => {
    test.setTimeout(60000);

    // Invocation of Skyramp Client
    const client = new SkyrampClient();
    // Request Body
    const ownersPostRequestBody: string = `{
        "address": "3 Test St.",
        "city": "Madison",
        "firstName": "Skyramp",
        "lastName": "Range",
        "telephone": "6085550003"
    }`;

    const ownersPostResponse = await createOwner(client, URL_localhost, ownersPostRequestBody);

    try {

        // Generated Assertions
        expect(ownersPostResponse.statusCode, 'status code').toBe(201);
        expect(getResponseValue(ownersPostResponse, "firstName"), 'firstName').toBe("Skyramp");
        expect(getResponseValue(ownersPostResponse, "lastName"), 'lastName').toBe("Range");
        expect(getResponseValue(ownersPostResponse, "address"), 'address').toBe("3 Test St.");
        expect(getResponseValue(ownersPostResponse, "city"), 'city').toBe("Madison");
        expect(getResponseValue(ownersPostResponse, "telephone"), 'telephone').toBe("6085550003");
        expect(getResponseValue(ownersPostResponse, "id"), 'id').toBeGreaterThan(0);

        // Request Body
        const petsPostRequestBody: string = `{
            "birthDate": "2020-01-01",
            "name": "Range",
            "type": {
                "id": 1,
                "name": "cat"
            }
        }`;

        const petsPostResponse = await createPet(client, URL_localhost, getResponseValue(ownersPostResponse, "id"), petsPostRequestBody);

        // Generated Assertions
        expect(petsPostResponse.statusCode, 'status code').toBe(201);
        expect(getResponseValue(petsPostResponse, "name"), 'name').toBe("Range");
        expect(getResponseValue(petsPostResponse, "birthDate"), 'birthDate').toBe("2020-01-01");
        expect(getResponseValue(petsPostResponse, "type.name"), 'type.name').toBe("cat");
        expect(getResponseValue(petsPostResponse, "ownerId"), 'ownerId').toBe(getResponseValue(ownersPostResponse, "id"));

        // Request Body
        const visitsPostRequestBody: string = `{
            "date": "2026-05-01",
            "description": "in range",
            "durationMinutes": 30
        }`;

        // Request Body
        const visitsPost1RequestBody: string = `{
            "date": "2026-05-01",
            "description": "below minimum",
            "durationMinutes": 4
        }`;

        // Request Body
        const visitsPost2RequestBody: string = `{
            "date": "2026-05-01",
            "description": "above maximum",
            "durationMinutes": 241
        }`;

        // Request Body
        const visitsPost3RequestBody: string = `{
            "date": "2026-05-01",
            "description": "at minimum",
            "durationMinutes": 5
        }`;

        // Request Body
        const visitsPost4RequestBody: string = `{
            "date": "2026-05-01",
            "description": "at maximum",
            "durationMinutes": 240
        }`;

        const [visitsPostResponse, visitsPost1Response, visitsPost2Response, visitsPost3Response, visitsPost4Response] = await Promise.all([
            createVisit(client, URL_localhost, getResponseValue(ownersPostResponse, "id"), getResponseValue(petsPostResponse, "id"), visitsPostRequestBody, 201),
            createVisit(client, URL_localhost, getResponseValue(ownersPostResponse, "id"), getResponseValue(petsPostResponse, "id"), visitsPost1RequestBody, 400),
            createVisit(client, URL_localhost, getResponseValue(ownersPostResponse, "id"), getResponseValue(petsPostResponse, "id"), visitsPost2RequestBody, 400),
            createVisit(client, URL_localhost, getResponseValue(ownersPostResponse, "id"), getResponseValue(petsPostResponse, "id"), visitsPost3RequestBody, 201),
            createVisit(client, URL_localhost, getResponseValue(ownersPostResponse, "id"), getResponseValue(petsPostResponse, "id"), visitsPost4RequestBody, 201),
        ]);

        // Generated Assertions
        expect(visitsPostResponse.statusCode, 'status code').toBe(201);
        expect(getResponseValue(visitsPostResponse, "durationMinutes"), 'durationMinutes').toBe(30);
        expect(getResponseValue(visitsPostResponse, "description"), 'description').toBe("in range");
        expect(getResponseValue(visitsPostResponse, "petId"), 'petId').toBe(getResponseValue(petsPostResponse, "id"));
        // Generated Assertions
        expect(visitsPost1Response.statusCode, 'status code').toBe(400);
        expect(getResponseValue(visitsPost1Response, "status"), 'status').toBe(400);
        expect(getResponseValue(visitsPost1Response, "title"), 'title').toBe("MethodArgumentNotValidException");
        expect(getResponseValue(visitsPost1Response, "detail"), 'detail').toBe("The request contains invalid or missing parameters");
        expect(getResponseValue(visitsPost1Response, "schemaValidationErrors.0.field"), 'field').toBe("durationMinutes");
        expect(getResponseValue(visitsPost1Response, "schemaValidationErrors.0.rejectedValue"), 'rejectedValue').toBe("4");
        expect(getResponseValue(visitsPost1Response, "schemaValidationErrors.0.message"), 'validation message').toContain("durationMinutes");
        expect(getResponseValue(visitsPost1Response, "schemaValidationErrors.1"), 'only durationMinutes was rejected').toBeNull();
        // The rejected create must not have produced a visit.
        expect(getResponseValue(visitsPost1Response, "id"), 'id').toBeNull();
        // Generated Assertions
        expect(visitsPost2Response.statusCode, 'status code').toBe(400);
        expect(getResponseValue(visitsPost2Response, "status"), 'status').toBe(400);
        expect(getResponseValue(visitsPost2Response, "title"), 'title').toBe("MethodArgumentNotValidException");
        expect(getResponseValue(visitsPost2Response, "detail"), 'detail').toBe("The request contains invalid or missing parameters");
        expect(getResponseValue(visitsPost2Response, "schemaValidationErrors.0.field"), 'field').toBe("durationMinutes");
        expect(getResponseValue(visitsPost2Response, "schemaValidationErrors.0.rejectedValue"), 'rejectedValue').toBe("241");
        expect(getResponseValue(visitsPost2Response, "schemaValidationErrors.0.message"), 'validation message').toContain("durationMinutes");
        expect(getResponseValue(visitsPost2Response, "schemaValidationErrors.1"), 'only durationMinutes was rejected').toBeNull();
        expect(getResponseValue(visitsPost2Response, "id"), 'id').toBeNull();
        // Generated Assertions
        expect(visitsPost3Response.statusCode, 'status code').toBe(201);
        expect(getResponseValue(visitsPost3Response, "durationMinutes"), 'durationMinutes').toBe(5);
        expect(getResponseValue(visitsPost3Response, "description"), 'description').toBe("at minimum");
        // Generated Assertions
        expect(visitsPost4Response.statusCode, 'status code').toBe(201);
        expect(getResponseValue(visitsPost4Response, "durationMinutes"), 'durationMinutes').toBe(240);
        expect(getResponseValue(visitsPost4Response, "description"), 'description').toBe("at maximum");

        // Request Body
        const visitsVisitsPutRequestBody: string = `{
            "date": "2026-05-01",
            "description": "in range",
            "durationMinutes": 2
        }`;

        const visitsVisitsPutResponse = await updateVisit(client, URL_localhost, getResponseValue(visitsPostResponse, "id"), visitsVisitsPutRequestBody, 400);

        // Generated Assertions
        expect(visitsVisitsPutResponse.statusCode, 'status code').toBe(400);
        expect(getResponseValue(visitsVisitsPutResponse, "status"), 'status').toBe(400);
        expect(getResponseValue(visitsVisitsPutResponse, "title"), 'title').toBe("MethodArgumentNotValidException");
        expect(getResponseValue(visitsVisitsPutResponse, "detail"), 'detail').toBe("The request contains invalid or missing parameters");
        expect(getResponseValue(visitsVisitsPutResponse, "schemaValidationErrors.0.field"), 'field').toBe("durationMinutes");
        expect(getResponseValue(visitsVisitsPutResponse, "schemaValidationErrors.0.rejectedValue"), 'rejectedValue').toBe("2");
        expect(getResponseValue(visitsVisitsPutResponse, "schemaValidationErrors.0.message"), 'validation message').toContain("durationMinutes");
        expect(getResponseValue(visitsVisitsPutResponse, "schemaValidationErrors.1"), 'only durationMinutes was rejected').toBeNull();
    } finally {
        const ownersOwnersDeleteResponse = await deleteOwner(client, URL_localhost, getResponseValue(ownersPostResponse, "id"));

        // Generated Assertions
        expect(ownersOwnersDeleteResponse.statusCode, 'status code').toBe(204);
    }
});
